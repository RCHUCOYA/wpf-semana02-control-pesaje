<?xml version="1.0"?>
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="WPF_SEMANA02.Views.ReporteIngresosWindow" Title="Reporte de ingresos" Height="520" Width="820" WindowStartupLocation="CenterOwner">
  <Grid Margin="35">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel>
      <TextBlock Text="Reporte de Ingresos" FontSize="28" FontWeight="Bold"/>
      <TextBlock Text="Operaciones registradas temporalmente durante la sesi&#xF3;n." Foreground="#64748B" Margin="0,5,0,0"/>
    </StackPanel>
    <DataGrid x:Name="dgIngresos" Grid.Row="1">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}"/>
        <DataGridTextColumn Header="Peso" Binding="{Binding Peso}"/>
        <DataGridTextColumn Header="Tipo de veh&#xED;culo" Binding="{Binding TipoVehiculo}"/>
        <DataGridTextColumn Header="Transportista" Binding="{Binding Transportista}"/>
      </DataGrid.Columns>
    </DataGrid>
    <Button Grid.Row="2" HorizontalAlignment="Left" Content="Volver" Style="{StaticResource BotonSecundario}" Click="Volver_Click"/>
  </Grid>
</Window>
